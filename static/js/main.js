// Main JS file for TechTrend Tracker
// Currently handles Dark Mode logic via inline script in base.html to prevent flash of wrong theme.
// Additional interactive logic can go here.

document.addEventListener('DOMContentLoaded', () => {
    console.log('TechTrend Tracker loaded.');
});
